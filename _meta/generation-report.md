# README generation report

- 日期：2026-08-09
- 语言：简体中文
- 范围：按窗口、整屏、局部、全局设定、导出设置重构根目录 README，并为三个录制模式配实机截图。
- 行文：沿用原 README 的标题、短路径说明、模式描述、导出说明、安装与隐私措辞，只为新增功能补写必要内容。
- 代码基线：`2e96e9c`（局部录像与鼠标效果实现）。

## 运行核验

- 从当前源码构建并启动 macOS App。
- 确认主界面存在“浏览器窗口 / 整个屏幕 / 局部录像”三个入口。
- 分别进入浏览器窗口和整个屏幕模式，确认来源选择与全局选项，并捕获对应实机截图。
- 进入局部录像，确认 7 个比例入口、大小框边角、柔和圆角暗角、聚焦蒙版、电脑声音、人声和录制鼠标控件。
- 开启聚焦蒙版，在无私人信息的中性测试画面上确认圆角内框、框内高亮以及框外灰度压暗效果。
- README 中所有界面图片均来自实际运行 App，不使用生成式 UI 图。

## 截图与隐私

- 发布截图只包含 Snap Recorder 界面或中性测试画面。
- 浏览器窗口与整个屏幕截图重新以 Retina 尺寸导出为 1120 × 1172 PNG，并保留 alpha 透明通道；局部模式主界面与导出界面同样使用 1120px 宽的透明 PNG。
- 已检查主界面截图的底部圆角，圆角外区域保持透明，不会在 GitHub 深色背景上出现白色尖角。
- 一次包含其他桌面内容的候选截图已丢弃，未放入仓库或最终文档。
- 截图构建临时允许系统捕获 App 窗口；完成后源码已恢复为 `sharingType = .none`，正式录制仍不会把控制界面录入成片。

## 未覆盖项

- 未在本次文档任务中逐一导出所有“画质 × 人声分轨”组合。
- 未执行多显示器切换验证；README 明确局部录像以主屏幕为范围。
- DRM 内容由 macOS 控制，仍可能显示黑屏。

## 文档校验

- `git diff --check` 通过。
- 用户手册校验器已读取 `_meta/evidence.json`，识别到 7 条声明和 4 条运行确认。
- README 为 GitHub 首页文档，按用户指定的五段结构编排，并用 HTML 居中标题与截图；因此没有套用校验器要求的独立手册章节（Getting Started、Features、Workflows、Troubleshooting、FAQ），HTML 图片也不会被该校验器计入 Markdown 图片数量。
